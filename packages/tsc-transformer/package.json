{
  "name": "@typescript-nameof/tsc-transformer",
  "version": "0.0.1",
  "description": "ts-nameof - TypeScript compiler transforms for ts-nameof packages.",
  "main": "./dist/index.js",
  "author": "The typescript-nameof Team (https://github.com/typescript-nameof",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "rimraf dist && tsc --b --clean",
    "build": "tsc --b",
    "test": "npm run build && mocha",
    "test:debug": "npm run build && mocha"
  },
  "dependencies": {
    "@typescript-nameof/common": "^0.0.1",
    "@typescript-nameof/tests-common": "*"
  },
  "devDependencies": {
    "mocha": "^10.2.0",
    "rimraf": "^4.3.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "peerDependencies": {
    "typescript": "*"
  }
}
