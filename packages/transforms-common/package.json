{
  "name": "@ts-nameof/transforms-common",
  "version": "4.2.1",
  "description": "ts-nameof - Common code for transforms.",
  "main": "./dist/index.js",
  "author": "David Sherret",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "rimraf dist && tsc --b --clean",
    "build": "tsc --b",
    "test": "npm run build && mocha",
    "test:debug": "npm run build && mocha"
  },
  "dependencies": {
    "@ts-nameof/common": "^4.2.1"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.1",
    "@types/node": "^18.14.6",
    "mocha": "^10.2.0",
    "rimraf": "^4.3.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}
