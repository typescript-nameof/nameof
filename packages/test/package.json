{
  "name": "test",
  "private": true,
  "scripts": {
    "build-ts": "tsc --build .",
    "watch-ts": "npm run build-ts -- --watch",
    "build-plugin": "babel --extensions .ts src --out-dir lib/plugin",
    "watch-plugin": "npm run build-plugin -- --watch"
  },
  "devDependencies": {
    "@types/nameof": "file:../types",
    "ts-nameof": "^5.0.0",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "@babel/cli": "^7.21.0",
    "@babel/core": "^7.21.0",
    "@babel/preset-typescript": "^7.21.0"
  }
}
